import { Observable } from "rxjs";

export interface LoginModel{
        nome: string;
        route: string;
        icon: string;
        role: string;
        title: string;
        expires: string;

}