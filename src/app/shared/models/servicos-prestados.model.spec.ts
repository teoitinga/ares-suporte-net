import { ServicosPrestados.Model } from './servicos-prestados.model';

describe('ServicosPrestados.Model', () => {
  it('should create an instance', () => {
    expect(new ServicosPrestados.Model()).toBeTruthy();
  });
});
