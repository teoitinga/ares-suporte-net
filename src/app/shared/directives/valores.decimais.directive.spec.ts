import { ValoresDecimaisDirective } from './valores.decimais.directive';

describe('Valores.DecimaisDirective', () => {
  it('should create an instance', () => {
    const directive = new ValoresDecimaisDirective();
    expect(directive).toBeTruthy();
  });
});
