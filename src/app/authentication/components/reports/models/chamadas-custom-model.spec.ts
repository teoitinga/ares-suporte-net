import { ChamadasCustomModel } from './chamadas-custom-model';

describe('ChamadasCustomModel', () => {
  it('should create an instance', () => {
    expect(new ChamadasCustomModel()).toBeTruthy();
  });
});
